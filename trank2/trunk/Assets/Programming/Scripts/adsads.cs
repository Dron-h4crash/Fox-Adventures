using UnityEngine;
using System.Collections;

public class adsads : MonoBehaviour {

	// Use this for initialization
	void Start () {
        GuiActions.NextWeapon();
        GuiActions.NextWeapon();
        GuiActions.NextWeapon();
        GuiActions.NextWeapon();

	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
